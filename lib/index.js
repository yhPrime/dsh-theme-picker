/** Host loader entry for the browser-only theme picker plugin. */
/** Provides no host-side behavior: the client half talks to the theme
 *  service and the dsh-market HTTP routes directly. */
function apply() {}
export { apply };
